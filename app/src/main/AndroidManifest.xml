<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" >

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="moe.shizuku.manager.permission.API_V23" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Android 11+ package visibility for the supported Shizuku-compatible
         managers. Official Shizuku remains the default; Shevery is optional. -->
    <queries>
        <package android:name="moe.shizuku.privileged.api" />
        <package android:name="com.hamondev.shevery" />
    </queries>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GAMA"
        android:enableOnBackInvokedCallback="true" >

        <!-- Declares support for the current Shizuku/Sui client protocol so
             GAMA is discoverable in the authorization manager. -->
        <meta-data
            android:name="moe.shizuku.client.V3_SUPPORT"
            android:value="true" />

        <!-- Exported because Android must launch the app from the launcher icon. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.GAMA"
            android:enableOnBackInvokedCallback="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ── Shizuku registration ──────────────────────────────────────── -->
        <!-- Exported for the Shizuku client handshake; protected by Shizuku's
             cross-user interaction permission and fixed authority. -->
        <provider
            android:name="rikka.shizuku.ShizukuProvider"
            android:authorities="${applicationId}.shizuku"
            android:multiprocess="false"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.INTERACT_ACROSS_USERS_FULL" />

        <!-- ── Tasker Integration ────────────────────────────────────────── -->
        <!-- Exported intentionally: Tasker sends the authenticated renderer
             command through this receiver; TaskerAuth rejects unknown callers. -->
        <receiver
            android:name=".TaskerReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.popovicialinc.gama.ACTION_SET_RENDERER" />
            </intent-filter>
        </receiver>

        <!-- ── Boot Receiver — re-applies renderer prop after every reboot ── -->
        <!-- Exported because Android delivers BOOT_COMPLETED broadcasts to it;
             it accepts only the documented system boot actions. -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>

        <!-- ── Quick Settings Tile: Renderer toggle (Vulkan ↔ OpenGL) ────── -->
        <!-- Exported because SystemUI binds QS tile services through this
             action; the framework enforces BIND_QUICK_SETTINGS_TILE. -->
        <service
            android:name=".RendererToggleTileService"
            android:exported="true"
            android:label="@string/tile_label"
            android:icon="@drawable/ic_tile"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>


    </application>

</manifest>
